name: Deploy Vite Site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22 # Updated to use the latest Node runner standard

      - name: Install dependencies
        run: npm install # Changed from 'npm ci' to 'npm install' to handle split folders safely
        working-directory: portfolio

      - name: Build project
        run: npm run build
        working-directory: portfolio

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './portfolio/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
